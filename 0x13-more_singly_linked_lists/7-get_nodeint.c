#include "lists.h"

/**
* get_nodeint_at_index - free
* @head: h
* @index: index
* Return: void
*/
listint_t *get_nodeint_at_index(listint_t *head, unsigned int index)
{
listint_t *tmp;
unsigned int i;
temp = head;
for (i = 0; node && i < index; n++;)
node = node->next;
return (node);
}
